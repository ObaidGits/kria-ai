# F5.5 Evidence Status — Observability, Pressure/Offline, Threat, and Fault Hardening

**Run:** `run-001`  
**Gate:** F5  
**Last updated:** 2026-07-29

---

## Summary

| Subtask | Description | Status | Evidence Path |
|---------|-------------|--------|--------------|
| 5.5.1 | Observability redaction allowlist inventory | ✅ Pass | `reports/observability-redaction.json`, `security/redaction-timing.json` |
| 5.5.2 | Seeded protected-token/embedding/hidden-ID scans | ⚠️ Unit-level Pass / Hardware Deferred | `reports/paired-world-scan.json` |
| 5.5.3 | Telemetry CPU/latency/storage overhead measurement | ✅ Pass | `reports/telemetry-sampling.json`, `performance/observability-overhead.json` |
| 5.5.4 | Network/embedder/LLM/model/server/worker fault campaign | ⚠️ Unit-level Pass / Hardware Deferred | `reports/fault-campaign-extended.json` |
| 5.5.5 | Battery/power/memory/thermal pressure scenarios | ⚠️ Unit-level Pass / Hardware Deferred | `reports/resource-pressure-campaign.json`, `traces/offline-pressure/` |
| 5.5.6 | Threat review (all 6 areas) | ✅ Pass | `reviews/threat.json`, `reports/threat-review.json` |

---

## What Is Implemented (Structural/Unit Level)

### 5.5.1 — Observability Redaction ✅

- Full allowlist audit of all `tracing::*` callsites in `crates/kria-core/src/memory/`
- **Violations found: 0** — no memory content, embeddings, or hidden IDs in any log field
- Structured allowlist schema documented in `security/redaction-timing.json`
- `SchedulerMetrics` / `SchedulerMetricsSnapshot` are physically u64-only (type system enforcement)
- `MemoryHealthReport` exposes only aggregate counts + `crypto_shred_capability` constant
- `cargo check -p kria-core` passes

### 5.5.3 — Telemetry Overhead ✅

- Overhead estimate: **0.005% CPU** (200× under the 1% budget)
- Mechanism: `tracing` static level filter — single `Relaxed` atomic load per disabled callsite
- `SchedulerMetrics` counters: single `fetch_add(Relaxed)` per call — ≤2ns per record
- Storage overhead: 56 bytes in-process (7 × `AtomicU64`) — zero disk writes from observability
- Security/recovery events preserved unconditionally (WARN/ERROR survive any INFO-or-higher filter)

### 5.5.6 — Threat Review ✅

Six threat areas reviewed — **0 critical, 0 high findings:**

1. **Server boundary**: Tauri/Axum thin adapters, AuthorityCommandBus gate, loopback-only default
2. **Policy non-interference**: 9/9 paired-world tests pass, zero cross-namespace data leaks
3. **Prompt-injection fencing**: local-only deployment, governed retrieval pipeline, audit trail
4. **Lifecycle/crypto truth**: all 7 surfaces consistently state "pending crypto erasure" — 0 overclaims
5. **Caches/cursors/logs**: policy_hash in cache/cursor keys, 0 content in logs, 0 deleted residue
6. **Local recovery ownership**: RecoveryMode disables writes unconditionally, local-only recovery

---

## What Is Deferred (Hardware Campaigns)

Per `dev-context.md`: _"heavy fault-injection campaigns require actual hardware runs"_. KRIA is a
pre-production single-developer project on one laptop — no production hardware target exists yet.

### 5.5.2 — Seeded Scan (Hardware) ⚠️ DEFERRED

**What exists:** Unit-level paired-world scan (9/9 pass) demonstrating namespace isolation  
**What's missing:** Actual hardware run of seeded protected-token scan across Tauri/server/search/
graph/path/predict/export/trace/patch surfaces with real process injection  
**Blocking condition:** Hardware run required per dev-context.md

### 5.5.4 — Full Fault Campaign (Hardware) ⚠️ DEFERRED

**What exists:** Unit-level coverage of all 6 fault classes (disconnect, deadline/cancel, malformed,
cursor expiry, patch disorder, optional context loss) citing Sections 1–17 of `memory_graph_v2.rs`  
**What's missing:** End-to-end fault injection with real network interface drop, OS-level process kill,
actual TCP timeout under load  
**Blocking condition:** Hardware run required per dev-context.md

### 5.5.5 — Hardware Pressure (Battery/Thermal) ⚠️ DEFERRED

**What exists:** Quality-ladder degradation (S9.12), burst queue overflow (S4.2), foreground
preemption (0.312ms vs 100ms budget), heap/RSS steady-band  
**What's missing:** Actual hardware power-saver mode, thermal throttle activation, GPU/model
concurrent pressure traces  
**Trace directory:** `traces/offline-pressure/` (created, README documents acceptance criteria)  
**Blocking condition:** Hardware run required per dev-context.md

---

## Residual Risks

| Risk | Level | Disposition |
|------|-------|-------------|
| Plaintext content after hard delete | Low | Accepted — all surfaces honest (`CRYPTO_SHRED_CAPABILITY='unavailable'`). Documented in `reviews/threat.json`. |
| Seeded-scan hardware not yet run | Low | Accepted for pre-production — unit-level namespace isolation proven. Will require hardware run before public release. |

---

## Evidence Path Cross-Reference (spec vs actual)

| Spec-required path | Actual path | Status |
|-------------------|-------------|--------|
| `security/redaction-timing.json` | `security/redaction-timing.json` | ✅ Created |
| `performance/observability-overhead.json` | `performance/observability-overhead.json` | ✅ Created |
| `traces/offline-pressure/` | `traces/offline-pressure/` | ✅ Created (deferred placeholder + README) |
| `reviews/threat.json` | `reviews/threat.json` | ✅ Created |
| `reports/observability-redaction.json` | `reports/observability-redaction.json` | ✅ Pre-existing |
| `reports/paired-world-scan.json` | `reports/paired-world-scan.json` | ✅ Pre-existing |
| `reports/telemetry-sampling.json` | `reports/telemetry-sampling.json` | ✅ Pre-existing |
| `reports/fault-campaign-extended.json` | `reports/fault-campaign-extended.json` | ✅ Pre-existing |
| `reports/resource-pressure-campaign.json` | `reports/resource-pressure-campaign.json` | ✅ Pre-existing |
| `reports/threat-review.json` | `reports/threat-review.json` | ✅ Pre-existing (flat layout) |
