# Governed feature packages

Every new UI feature package belongs in `src/features/<feature-id>/` and must include `feature.governance.json` as documented in `docs/UI_EXPANSION_GOVERNANCE.md`.

The expansion lint scans every child directory here. Do not place a top-level feature package elsewhere to evade governance.
