{
  "type": "object",
  "additionalProperties": false,
  "required": ["sub_goals"],
  "properties": {
    "sub_goals": {
      "type": "array",
      "minItems": 1,
      "maxItems": 12,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["intent", "kind"],
        "properties": {
          "intent": { "type": "string" },
          "kind": {
            "type": "string",
            "enum": ["open_app", "click", "type", "navigate", "run_command", "write_file", "read_output", "verify", "other"]
          },
          "target_hint": { "type": ["string", "null"] },
          "expect_contains": { "type": ["string", "null"] }
        }
      }
    }
  }
}
