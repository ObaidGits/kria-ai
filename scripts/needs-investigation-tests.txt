# Failures that are NOT explained yet.
#
# `scripts/gate.sh` prints anything listed here as a loud WARNING block with a
# count, because an unexplained failure is technical debt and should feel like it.
#
# Rules:
#   * Moving a line OUT of this file requires an actual answer, not a shrug.
#   * Real bug -> fix the code, delete the line.
#   * Stale test -> fix the test WITH A COMMENT saying why, delete the line.
#   * Environmental -> move it to known-failing-tests.txt with its reason.
#
# ── EMPTY ────────────────────────────────────────────────────────────────────
# All 16 entries have been triaged. Outcomes:
#
#   REAL BUGS FIXED (4)
#     * Content search ("search inside .py files for the pattern ...") routed to
#       search_files, which searches FILENAMES — a silently wrong answer.
#     * "What is my CPU stats?" routed to execute_bash instead of get_cpu_usage:
#       wrong tool, and a needless escalation from a read to a shell execution.
#     * Hinglish "mujhe yaad rakhna: ..." routed to gw_calendar_search, so a
#       request to REMEMBER something searched the calendar and stored nothing.
#     * SkillRegistry::list_installed returned uninstalled (soft-deleted) skills
#       and reported every skill's status as Active regardless of real state.
#
#   STALE TESTS CORRECTED, each with a comment giving the evidence (10)
#     Config defaults (theme light, model catalogue), risk tiers now taken from
#     the frozen contract (read_file, get_clipboard, kill_process RED; sudo
#     root destruction BLACK), a service query now using a dedicated tool, a
#     prompt assertion moved to the tool description where the guidance lives,
#     and two telemetry tests that asserted tracing::Level ordering BACKWARDS
#     and tested the tracing crate rather than KRIA.
#
#   OBSOLETE TESTS DELETED (2)
#     integration_packages.rs — written against the pre-OS-control architecture
#     where handlers reached the host directly. The properties it asserted are
#     covered at the provider layer (os_control_packages_lifecycle.rs) and the
#     handler envelope is covered in four other suites.

# ── Frontend (added to the gate in the Converse work) ────────────────────────
# A CSS transition exceeds the motion budget. PRE-EXISTING — confirmed by running
# the suite with the Converse changes stashed. It went unnoticed because the UI
# suite was outside this gate until now.
src/platform/motionBudget.test.ts
