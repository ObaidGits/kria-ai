# Negative golden input — reverse orphan (role: validation.md)

Planted defect: suite `V-ORPHAN-01` is defined but no requirement or decision
governs it. Expected diagnostic kind `reverse_orphan` for id `V-ORPHAN-01`.

| Suite | Behavior |
|---|---|
| `V-ORPHAN-01` | Defined suite that no MGR/MGD ledger row maps to |
